https://leetcode.com/problems/maximum-subarray/
SOLUTION:
class Solution {
public:
    int maxSubArray(vector<int>& nums) {
        //kadana's rule//STRRIVER
    //     int sum=0;int maxi=INT_MIN;   //maxi=nums[0];
    //     for(int i=0;i<nums.size();i++){
    //         sum+=nums[i];
              MAXI=MAX(MAXI,SUM);          //same
    //         if(sum<0){
    //             sum=0;
    //         }

    //     }
    //     return maxi;
    // }

     
